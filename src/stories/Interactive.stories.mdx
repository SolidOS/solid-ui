import * as UI from '../../lib'

import { Canvas, Meta, Story } from '@storybook/addon-docs/blocks'

<Meta title="Interactive"/>

## Ask name

[API documentation](https://solid.github.io/solid-ui/Documentation/api/modules/_widgets_buttons_.html#askname)

<Canvas>
  <Story name="Ask name">
    {() => {
      const div = document.createElement("div");
      UI.widgets.askName(document, UI.store, div)
        .then(name => window.alert(`You picked "${name}"`))
      return div
    }
    }
  </Story>
</Canvas>

## Attachment list

Component that displays a list of resources, for instance the attachments of a message, or the various documents related to a meeting.
Accepts dropping URLs onto it to add attachments to it.

[API documentation](https://solid.github.io/solid-ui/Documentation/api/modules/_widgets_buttons_.html#attachmentlist)

<Canvas>
  <Story name="Attachment list">
    {() => {
      const div = document.createElement("div");
      UI.store.add(
        UI.rdf.namedNode('https://example-user.inrupt.net/public/some-message.ttl#this'),
        UI.ns.wf('attachment'),
        UI.rdf.namedNode('http://example.com/#some-attachment')
      )
      UI.widgets.attachmentList(document, UI.rdf.namedNode('https://example-user.inrupt.net/public/some-message.ttl' +
        '#this'), div)
      return div
    }
    }
  </Story>
</Canvas>

## personTR

A TR to represent a draggable person, etc in a list

[API documentation](https://solid.github.io/solid-ui/Documentation/api/modules/_widgets_buttons_.html#persontr)

<Canvas>
  <Story name="Person TR">
    {() => {
      const michiel = UI.rdf.namedNode('https://michielbdejong.inrupt.net/profile/card#me')
      return UI.widgets.personTR(document, null, michiel)
    }
    }
  </Story>
</Canvas>

